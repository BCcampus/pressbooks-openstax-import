sudo: enabled
language: php

notifications:
  email:
    on_success: never
    on_failure: change

branches:
  only:
    - dev

deploy:
  provider: releases
  api_key: "GITHUB OAUTH TOKEN"
  file: "FILE TO UPLOAD"
  skip_cleanup: true
  on:
    tags: true
